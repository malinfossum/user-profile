## Assignment

Create a webpage where the user can enter:

- name
- age
- favorite color

Create a function that takes these values as parameters and:

- displays the name and age in a text on the page
- changes the background color based on the favorite color

The function should be reusable and work with different values each time it is called.
